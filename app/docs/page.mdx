import { createElement } from "react";

export const metadata = { title: "Introduction" };

export default function Introduction() {
  return createElement(
    "article",
    { className: "space-y-4 max-w-xl" },
    createElement(
      "h1",
      { className: "text-2xl font-semibold" },
      "Introduction",
    ),
    createElement(
      "p",
      { className: "text-muted-foreground" },
      "Neon-like docs with ",
      createElement("strong", null, "MDX"),
      ", shadcn/ui, and floating chat.",
    ),
    createElement(
      "ul",
      { className: "space-y-2 text-sm" },
      createElement(
        "li",
        null,
        "Context Service: ",
        createElement("code", null, "/index"),
        ", ",
        createElement("code", null, "/search"),
        ", ",
        createElement("code", null, "/chat"),
      ),
      createElement("li", null, "Providers: ZAI / OpenRouter / Mistral"),
    ),
  );
}
