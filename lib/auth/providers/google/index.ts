// Google OAuth Provider
// Exports strategy and callback handler for Google authentication

export { GoogleStrategy, googleStrategy, GOOGLE_CONFIG } from "./strategy";
export { GoogleCallbackHandler, googleCallbackHandler } from "./callbacks";
